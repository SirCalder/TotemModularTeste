// Configurações da aplicação
// IMPORTANTE: Não commitar este arquivo no Git!
// Renomeie config.example.js para config.js e adicione suas chaves

const CONFIG = {
    GEMINI_API_KEY: 'YOUR_GOOGLE_GEMINI_API_KEY_HERE',
    
    // Outras configurações podem ir aqui
    APP_NAME: 'Secretaria Digital Amanhecer',
    VERSION: '1.0.0'
};

// Torna CONFIG disponível globalmente
window.APP_CONFIG = CONFIG;